"""Synthetic demo payer portal (FastAPI).

A tiny two-page app used only as a local target for the browser agent. It is
clearly labeled synthetic in every page. It does not resemble, proxy, or
connect to any real payer system, and it holds only synthetic case data.
"""
