# schemas

Shared Pydantic models: cases, extractions, payer policies, decisions, approval/audit events, PHI redaction helpers, and synthetic seed data specs.

Used by the agent, MCP servers, eval harness, and UI. Labels for eval live under `data/labels/` (not imported at agent runtime).
