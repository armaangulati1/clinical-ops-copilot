# Environment
.env
.venv/
venv/

# Agent run logs
data/runs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local HAPI FHIR Postgres data
fhir/pgdata/

# Synthea prebuilt JAR and generated output (gitignored)
synthea/
.java/

# Plaintext vendor blocklist. Only the SHA-256 digest file is committed; the
# names themselves must never enter the repo or its history.
tests/vendor_blocklist.local.txt

# Generated TTS audio samples (say + ElevenLabs before/after comparison).
# Local playback evidence only; audio is never committed.
voice/samples/

# Local scratch/demo helpers (never commit)
demo1.py
demo2.py
demo_ui.py
demo_env.sh
demo_case_*.json
data/audit/demo_*.jsonl

# Local Airflow venv + metadata DB for the online-eval DAG (never committed)
.venv-airflow/
.airflow/

# Online-eval loop runtime state (traces, scored runs, cycles, alerts, baseline)
data/online_eval/
